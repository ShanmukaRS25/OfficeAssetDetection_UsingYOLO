ultralytics==8.3.145          # YOLOv8 / YOLO11 model + inference engine
opencv-python==4.10.0.84      # Video capture, frame drawing, codec support
numpy==1.26.4                 # Array ops (pinned for ultralytics compat)
Pillow==10.4.0                # Image save / screenshot handling

fastapi==0.115.6              # Async REST + WebSocket server
uvicorn[standard]==0.32.1     # ASGI server with auto-reload support
python-multipart==0.0.12      # Form / file upload handling

aiosqlite==0.20.0             # Async SQLite driver (detection logs)

python-dotenv==1.0.1          # Load .env config
httpx==0.27.2                 # Async HTTP client (health checks / tests)
